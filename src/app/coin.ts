export class Coin {
  /*private name: string;
  private baseAsset: string;
  private quoteAsset: string;
  private lastPrice: string;
  private volumn24h: string;
  private marketId: string;*/
  // tslint:disable-next-line:max-line-length
  constructor(public name: string, public baseAsset: string, public quoteAsset: string , public lastPrice: string, public volumn24h: string, public marketId: string ) {
  }
}
